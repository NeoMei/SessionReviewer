# 0.4.2 发布进度

## 当前结论

**0.4.2 已正式发布，正式 CLI 与 BRAT 插件升级及真实问答展示验收完成。** 发布目标为 `aa91c4b`；本记录中的早期阻断均为过程记录，不是当前发布状态。未宣称所有历史技术债或后续功能全部完成。

## 已完成

- 用户明确授权合并 main、发布 0.4.2、更新本地 CLI 和 Obsidian 插件。
- 发布准备提交 `be2fb479d01076c7ea2b5ebba34d08d9aaaec374` 已快进合并并推送 main；版本清单、升级映射、打包默认版本及中英文说明一致。
- 合并后完整 Go 测试退出 0（CLI 234.754s、scan 304.058s、zero-token 316.431s）；插件 322 项测试、lint 和构建通过。
- 本地 CLI 三个平台归档和插件包构建成功，SHA256SUMS 全部通过。
- 主目录回顾与草稿逐项比对未变。备份：`/Users/neomei/.local/share/session-reviewer-install-backups/release-0.4.2.rxqVSz`，含原回顾、草稿、CLI、插件。

## 发布阻断与修复

- 标签 `0.4.2` 已推送，标签对象 `40480763f056f4ae7c0cbe594b21c21077ab1de7`，目标为 `be2fb47`。
- 标签 CI：<https://github.com/NeoMei/SessionReviewer/actions/runs/34099636573>。三个环境均在同一跨语言回归用例报 `Test timed out in 5000ms`，发布 job 未执行，GitHub Release 不存在。
- 根因：测试在 Go 冷编译时仍使用 Vitest 默认 5 秒上限。普通空缓存本机耗时 4.89s；空缓存加 GOMAXPROCS=1 稳定复现同一失败，耗时 15.787s。
- 最小修复：仅该用例配置 150 秒上限，实际 Go 子进程限制 120 秒。产品超时、真实 Go 响应生成、TypeScript 解析和断言不变。
- 同样空缓存／单核条件修复后通过，耗时 18.51s；完整插件 322 项测试、lint、构建通过。独立限定复审无 Critical/Important/Minor 问题。

## 第一轮结束时的状态（已由后续授权推进）

远端标签已公开，不擅自移动或覆盖。需用户明确允许将尚未生成 Release 的 0.4.2 标签更新到修正提交，之后重新跑标签 CI、验证正式下载资产，再安装本地并做原生重载验收。
当前未上传替代资产、未绕过 CI、未把本地候选冒充正式版；实际本地安装仍是此前 0.4.1 修复候选。用户回顾与插件配置保持不变。

## 第二轮 CI 与门禁修复

- 用户确认继续后，main 与标签更新到 `3dad267680ac7324d43cfcf5c90575f35954442e`；标签对象 `1eb42412793c3082f7e89b260ac8eaca974b1795`。采用旧标签精确哈希的 force-with-lease，不盲目覆盖。
- 标签任务 `34100415885` 与 main 任务 `34100416067` 的插件检查均越过原先冷编译超时。main 的 Apple Silicon 整套检查通过。
- Intel 在非 race 的 65,538 条观测大扫描用例中触及默认每包 10 分钟上限；超时栈位于正常可运行的认证解码／哈希计算，未报告内容断言失败。上一正式版同包 nonrace 312.251s、race 1346.184s，本地当前完整 scan 包 304.058s。
- Windows 新增 inspect 测试只有 Home、缺少 LocalAppData，并写死 Unix 数据路径，导致三条实际命令边界测试报“数据目录不可用”。这是测试 fixture 与实际系统目录规则不一致；未更改产品路径规则。
- 修复 fixture：建库与执行共享同一 platform.Env，由 platform.DataDir 推导数据位置；保留三条真实命令测试所有断言。CI 的非 race 全量测试显式设置 30 分钟每包预算，与已有 race 预算一致，不删数据、不跳过测试。
- 本地 inspect 定点回归通过（1.799s），完整 65,538 条观测用例通过（56.592s）；独立限定复审无遗留问题。Windows／Intel 最终通过仍以修正后的 CI 为准。
- 修正后完整 `go test -timeout 30m ./...` 退出 0（CLI 281.674s、scan 348.932s、zero-token 261.642s）；Windows CLI 测试程序交叉编译通过。旧的失败标签构建已取消剩余工作，新标签构建必须完整重跑。

## 第三轮发布门禁

- main 与 `0.4.2` 标签目标均为 `aa91c4b98fa5373c8432dba765e8c486a15efbd0`；标签对象 `6f6557fe74c2b17cdb4ff50ec79f9c6a3f8503c5`，使用上一标签对象精确哈希保护更新。
- 标签 CI：<https://github.com/NeoMei/SessionReviewer/actions/runs/34103854311>；main CI：<https://github.com/NeoMei/SessionReviewer/actions/runs/34103854259>。
- Apple Silicon 标签任务完整通过，Intel 与 Windows 普通 Go 测试均通过，正在执行 race 检查。此处不作为正式发布完成证明。
- 安装前再次检查：真实项目 sync status 为 `in_sync=1`、`conflicted=0`、`malformed=0`、`blocked=0`、`derived_state=current`。
- 插件 `data.json` 与发布前备份 SHA256 一致：`0bbea507240d460db7a740404d8bc481c1eda0815a7b25d6eab8fa403f027b95`。BRAT 单插件指定版本更新入口已在 NeoMei-Docs 原生界面确认；尚未执行安装。

## 正式发布与本地验收（2026-09-07）

- 标签任务 `34103854311` 四个 job 全部成功：macOS Intel、macOS Apple Silicon、Windows x64、publish-release。未绕过发布门禁。
- GitHub 正式 Release：<https://github.com/NeoMei/SessionReviewer/releases/tag/0.4.2>，发布时间 `2026-09-07T10:02:14Z`，非 draft、非 prerelease，8 个资产齐全。发布说明已补上修复内容、升级方式与覆盖边界。
- 正式资产下载至 `/tmp/session-reviewer-official-0.4.2.aey75i`；SHA256SUMS 所列 7 项全部通过。插件 ZIP 恰含 `main.js`、`manifest.json`、`styles.css` 三个文件。
- 从正式 arm64 包安装 `/Users/neomei/.local/bin/session-reviewer`；与解包文件逐字节一致。`version --json` 返回 `0.4.2`、提交 `aa91c4b98fa5373c8432dba765e8c486a15efbd0`，二进制 SHA256 为 `0cd6f4be9c0f6b64ad578f0b395eb3c0bceddea89d404ea136439577433085b7`。
- 在 NeoMei-Docs 原生 Obsidian 执行 BRAT 单插件更新，目标 `NeoMei/SessionReviewer`，实际安装 manifest 从 `0.4.1` 变为 `0.4.2`。未手动覆盖插件文件，未更新其他插件。
- 安装后的 main.js 与 styles.css 分别匹配正式资产 SHA256 `ce2bffb29059f5a8cbb253727334da6072ae8a363d02ea82fdb1b822d152858f`、`ae61a92040ed40c4ae9946bf787ca5b6e963a459ada654eebb90cd9b547a2787`。BRAT 将 manifest JSON 压成一行，解析后对象与正式 manifest 深比较完全一致，不能将排版导致的文件哈希差异误判为版本不一致。
- 单独重载 SessionReviewer 后，原生界面实测 SessionReviewer → AgentWiki → SessionReviewer；正确重新显示目标 Session。问答首页 `1–20 / 78`、末页 `61–78 / 78` 正常。第 61 个问题消息末页为 `61–80 / 80`，截图确认 Agent 最终回答正文及末尾内容可见；完成后回到问答首页。
- 使用实际安装的正式 CLI 再次逐页校验：78 组问答、78 条用户消息、1,528 条 Agent 消息、66 条最终回答；95 次查询，单次最大 841ms、最大响应 64,171 bytes。全部源引用匹配，正文截断为 0，公开回顾文件与发布 generation 未变。
- 覆盖边界仍如实保留：5 条上下文包装、269 条超限源记录，`coverage.complete=false`；不能将本次全部已捕获消息校验通过描述成整个原始 Session 无边界恢复。
- 安装后 sync status 仍为 `in_sync=1`、零冲突／畸形／阻断，派生状态 `current`；插件 data.json 与发布前备份保持一致。本轮仅安装与读取验证，没有重新扫描或改写人工回顾。
- 同提交的独立 main CI `34103854259` 在此验收时两项 macOS job 已成功，Windows race 尚在运行；不以标签 CI 的成功冒充该独立运行已完成。正式发布依据是完整成功的标签流水线。
- 回滚备份保留在 `/Users/neomei/.local/share/session-reviewer-install-backups/release-0.4.2.rxqVSz`；未清理用户的其他工作区或草稿。
