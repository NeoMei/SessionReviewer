# Spec restoration acceptance ledger

## Authority and current state

User request: “去改好！” after rejecting 0.4.2 as non-compliant with the accepted UI spec.
Authority: `docs/superpowers/specs/2026-09-04-obsidian-project-context-navigation-design.md`, existing five subsystem plans, and approved `problem-context-layout-v3.html` / `evolution-detail-closed-loop.html` prototypes.
The user explicitly removed cross-platform expansion and old-project migration from required new work; preserve existing compatibility, do not implement migrations for this repair. Re-scan remains the accepted path. All other requirements remain binding.

Baseline: `5c5cb7a`; clean isolated worktree `.worktrees/codex-v4-scan-display`, branch `codex/spec-ui-restoration`. Baseline plugin tests: 22 files, 322 tests pass, 2026-09-07. Main's human review files and brainstorm prototypes are untouched.

**Overall status: NOT COMPLETE. No merge, release, or completion claim until every applicable requirement below has evidence.** A completed subtask is not product acceptance. No fabricated milestones, problem parents, verification, or price values may fill an empty page.

## Current delivery state — 2026-09-10 convergence

The user approved the functional-package convergence proposal in the current task: “好的，按照你建议的方式继续推进项目”. This supersedes the pause and the former microtask/repeated-full-review execution order. The accepted product requirements remain binding except the previously cancelled new cross-platform expansion and old-project migration. Historical authorization/checkpoints below and in `2026-09-08-release-execution.md` are evidence, not dispatch instructions.

This table is the only current work queue. Update rows in place; put detailed implementation/review outputs in the existing ignored SDD workspace and link durable evidence here. No new plan per small correction.

**Visual acceptance rejected by the user (2026-09-10).** The supplied decision-candidate screenshot shows an oversized generic gray card, weak text/action hierarchy, an exposed full Session UUID, adjacent undifferentiated buttons, and configuration/history/job status/native-document controls stacked without a coherent layout. Inspection confirms candidate/evidence/extraction sections mostly use unstyled section/p/div/button elements and the generic `sr-card`; they do not carry the approved prototypes' compact typography, grouped actions or visual hierarchy. Previous native evidence proves behavior/readback only. F2–F4 behavioral verification remains valid, but `PACKAGE VERIFIED` must not be read as accepted UI or product delivery. Current priority is to restore the decision page's visual structure against the accepted prototype language, then compare the actual native rendering; the OpenCode choice does not preempt this user correction. Screenshot preserved locally as existing SDD `ui-feedback-2026-09-10.png`. This rejection remains historical evidence; the correction below supersedes the no-fix state without claiming user visual acceptance.


**Decision UI correction and execution simplification (2026-09-10).** The user explicitly requested faster execution and UI improvements. Implemented compact decision/candidate cards, inline status/count, primary/secondary/quiet actions with spacing, short evidence labels retaining the full provider/Session in accessible text and tooltip, bounded evidence reading, grouped extraction/configuration, responsive edit forms, and a single grouped public footer. Source authentication, confirmation/save semantics, storage and explicit Agent invocation are unchanged. Current focused frontend validation: 5 files / 52 tests PASS (0.73s), TypeScript/build PASS, scoped lint PASS. Actual production-renderer harness passed at viewport960/pane898 and viewport430/pane388: no horizontal overflow; exact evidence read, draft edit/cancel, zero saves and zero Agent starts. Native Obsidian in the authorized temporary Vault loaded the current bundle/styles; formal decision edit → cancel closed correctly, the three footer actions are grouped, and the candidate/extraction layout was visually inspected. Native controlled stale evidence still disables confirmation. Evidence is retained locally in existing SDD `decision-ui-2026-09-10/` (`native.png`, wide/narrow screenshots, `result.json`, `check.mjs`). This is an implemented decision-page correction with rendered checks, not user-approved visual acceptance for all five pages. F3 visual approval remains pending; other pages and provider/release gaps remain open. No Go regression was repeated for this frontend-only change; prior backend evidence remains a baseline, not a new run. Only the temporary Vault plugin was updated, with its prior assets backed up; no daily Vault install, merge, push or release.

**Five-page UI convergence (2026-09-10, following “继续完成剩余任务”).** Unified theme-aware controls, text hierarchy and spacing across evolution, problems, decisions, Sessions and usage. Problem edit/move now expand on demand, with visible field labels and short source controls retaining full identity; evolution source detail is collapsible; Sessions has the matching page title and grouped search/launcher controls; pricing forms occupy the full price-entry width with responsive fields/actions and readable totals. Updated the existing pane fixture to keep the selected turn's user preview body null; the rendered check now requires the actual Agent answer, and hidden controls are allowed only inside collapsed details. Validation: full plugin integration 40 files / 588 tests PASS (11.13s), types/build PASS, lint 0 errors / 3 existing Agent-capitalization warnings. Actual renderer checked all five tabs at wide/narrow sizes and pane580/760/1200 plus viewport390/pane374; pricing draft open/cancel also fits a narrow pane. Native temporary Vault: problem edit expand/collapse, pricing supplement open/cancel, cached catalog query (248 entries), Sessions exact question/answer/3 actions/4 results, and evolution five-segment reading passed; unknown price stays unknown. Native screenshots and temporary harness are in `/tmp/session-reviewer-workspace-ui/`; original rejection remains retained above. Only the authorized temporary Vault has been updated. UI implementation now covers all five pages; user visual approval, real-provider resume and OpenCode source implementation remain separate. UI commit: `99e5fc1`. No backend code changed or Go full suite repeated.

**Remaining external acceptance boundary (2026-09-10).** Local OpenCode is still1.18.30; `session list --help` exposes max-count/JSON but no continuation option. The original direct-SQLite exclusion was superseded by the user’s later “确认” on 2026-09-10; see the approved amendment below. Implementation uses opt-in fixture sources pending separate live-store acceptance. For actual Codex resume, the existing temporary historical-project data root's launcher was backed up to `/tmp/session-reviewer-workspace-ui/codex-launcher-before.json`, then verified against the installed `codex-cli 0.150.1` executable. In the current native Obsidian UI, project `project-c927ddd3a91ef8d4` → exact Session `01a050c7-e37e-7823-b381-b3d53949edad` → open → explicit confirmation returned “打开请求已提交”. Computer Use rejected reading `com.apple.Terminal` for safety reasons. Thus this advances the real launch request but does not attest client resume. The user subsequently supplied screenshots showing the client and history, with the exact ID still truncated; no message or model prompt was sent. Claude/OpenCode real client resume remains unverified. Release/daily-Vault install still awaits the applicable completion and authorization gates.


**Screenshot follow-up and remaining-gate probe (2026-09-10).** The user's two Terminal screenshots show the installed Codex client, historical content, and a hooks-review panel (9 untrusted hooks). They support client-open/history-visible evidence; the title truncates the Session ID, so exact target resume remains unverified. No hook was trusted and no Terminal-access workaround was used. A bounded metadata-only probe of the effective Claude root `/Users/neomei/.claude` found 9 project directories / 17 top-level JSONL files and no SessionReviewer cwd in their first 40 records. This does not prove absence in other roots or later records; no matching historical sample was available in this probe, so controlled Claude acceptance is not relabeled historical acceptance. Existing five-page UI screenshots and the two user screenshots are now archived in ignored SDD `remaining-gates-2026-09-10/`, with hashes and probe limits in `evidence.json`. The UI images retain their original verification provenance; no code changed or previously passed suite was rerun.

**OpenCode amendment approved (2026-09-10).** The user replied “确认” to permitting the read-only SQLite adapter. The former prohibition is superseded by the amended design §8.3; implementation and isolated-fixture verification are authorized, live database acceptance remains separate. The previously proposed boundary follows for continuity. The approved change permits an additional read-only SQLite source adapter when the documented CLI cannot establish complete project discovery. Its scope is authenticated current-project Session discovery (including child Sessions), followed by the existing provider-neutral evidence/index/answer path. Require an explicit compatible schema, a consistent read snapshot including committed WAL data, bounded queries/time/memory, no writes/migrations/checkpoints to the source, no raw transcript sidecar, and no Agent invocation. Retain stable finished-turn prefixes, source-drift detection, exact project/Session identity, and truthful incomplete coverage on unsupported schema or exceeded limits. Prefer CLI where it can meet the same completeness contract; never combine two conflicting copies silently. Implementation should first use synthetic database fixtures covering over-100 Sessions, children, concurrent changes and unchanged source bytes; a subsequent authorized real-project read is a separate acceptance step. This boundary is now accepted and reflected in the design; the implementation checkpoint below supersedes its former unimplemented state. Live database inspection remains separate.

**OpenCode implementation and regression checkpoint (2026-09-10).** Added the opt-in read-only SQLite adapter, canonical source coordinates, full/retained answer integration, child discovery, stable finalized prefixes, typed shell/generic tool facts and honest usage diagnostics. Native Session IDs preserve case end-to-end. RAM snapshot tests cover committed WAL, active readers, observed mutation, source-byte preservation, invalid checksum and unsupported layouts; snapshot consistency is the documented optimistic double-capture boundary. Accepted-baseline regressions prove valid → unavailable → repaired usage publication via the strict `usage_refresh` relation while conversation references remain unchanged. Independent source/snapshot/integration review findings were corrected; final accounting-delta review PASS. No live user database was opened.

The one whole-Go integration attempt exited1 because its OpenCode scan test exposed mixed-case Session IDs; all other packages, including the expanded Gate A, passed. That failure is preserved in `f5-opencode-native/go-test.log`. After corrections, affected package checks passed: source0.590s, OpenCode5.742s, catalog3.799s, scan202.075s, memory2.202s; inspect72.933s and contextupdate17.649s covered their current integration. Final scoped vet and tidy-diff passed. Exact Gate A rerun after the two accounting reflection sites passed48.006s. The inventory adds known SQLite/VFS imports and exact reflection/scalar references; process restrictions are unchanged. Full-suite failures are not relabeled passes, and already-passing unaffected packages were not rerun. Evidence and scoped review reports are retained in existing ignored SDD `2026-09-08-release-execution/f5-opencode-native/`.

**OpenCode native fixture acceptance completed (2026-09-10).** The authorized temporary Vault now reads the synthetic SQLite project `project-71809e83e6f0c920`:2 complete Sessions including a child, root30 turns and child1 turn. Actual native controls reached pages1–20/30 and21–30/30, selected the exact turn30 answer, and displayed authenticated2 actions/3 results; hidden reasoning was absent. Usage totals465 Tokens, with unreviewed billing quantities and all-unknown subtotal correctly shown as待定. Original native failures revealed Session-ID casing gates in TS and CLI; dedicated Session-only fixes retain strict provider/project/generation/hash validation and exact case binding. Public `cli.Run` tests now traverse published summary/events/Q&A index and selected messages; focused CLI coverage13.376s and complete OpenCode9.110s passed. TS integration239 tests/types/build/lint passed; final unknown-billing display regression9 tests/build passed. Independent source, accounting, TS and CLI reviews PASS; controller inspected the bounded pricing display correction.

Final synthetic rescan `scan-d2f5580df656f8414b4115029b38f3d6` completed2/2 with0 issues and0 review tokens; the four Project/Vault publication hashes match pairwise. Source database remains61440 bytes with SHA256 `e941d89975672987f03ebb2a755e49d22194f91432995b6b550521462366a1a9`, unchanged after scan/read, with no WAL/SHM sidecars created. Candidate plugin SHA256 `286e2aa1f8a22c8a39ef8af2a7c55587d1d44fcf22e4c7037eaeadfb6430c87f`; only the temporary Vault was updated. Existing SDD `f5-opencode-native/native-proof.json`, native screenshots, first/last answer text, source-before/after and failure logs retain the evidence. This closes the approved adapter implementation and synthetic native acceptance. User visual approval, authorized live OpenCode store/current-project acceptance, exact real-provider client resume and release/daily-install gates remain open. No hook trust, live database access, main merge, push, release or daily-Vault install occurred.

**Real OpenCode store check and explicit snapshot budget (2026-09-10 continuation).** After the user continued, a bounded read-only adapter probe selected only the authenticated SessionReviewer main directory and current worktree. The actual database is1,074,999,296 bytes with1,928,192 bytes of WAL. The default512MiB payload bound rejected it before content access; an explicit2048MiB bound also rejected the temporary growth allocation, correctly counting captured buffers and expanded image together. Added `SESSION_REVIEWER_OPENCODE_SNAPSHOT_MIB` (default512, allowed1–4096 subject to platform integer range), with one immutable operation-local limit applied to file admission, WAL pages/commit size and aggregate expansion. Existing timeout, readonly VFS, checksum/identity checks and selected-content limits are unchanged. On this32GiB host, the explicit3072MiB probe succeeded; final measured adapter time803ms. This setting was scoped to acceptance commands, not saved to the daily environment.

Real store schema/snapshot/discovery passed with0 matching Sessions for both authorized project roots and0 issues. Database/WAL SHA256, size, inode and mtime matched before/after; SHM was stat-only (contents not opened) and metadata matched. No raw transcript copy, source mutation, Agent call or project-document publication occurred. This is real-store compatibility/no-write evidence, not real answer-reading acceptance. A specific project with existing OpenCode history has been requested from the user; no unrelated project content was inspected. Evidence: existing SDD `f5-opencode-live/probe-before.json`, `probe-after.json` (2048MiB rejection), `probe-final.json`, `source-before.json`, `source-after.json`, and content-free probe source. Regression first failed on ignored/invalid budgets, then focused snapshot tests and full affected OpenCode package passed2.353s; scoped vet and diff-check passed. Narrow independent review found no blocking issue. No whole-Go/frontend suite was repeated for this small source-budget correction.

| Package | User acceptance | State | Current blocker / evidence |
|---|---|---|---|
| F1 First use and real-data reading | Fresh init → scan → native Obsidian full Session/history/Q&A/evolution reading; human edit survives rescan | IN PROGRESS | 5100079 removed49 pure notifications; 7a99e58/1f4ca1c final native rescan excludes the pure interruption notice too, leaving20+22 questions; goal/source coordinates/answers preserved (green-5). 92d3b45 fixes fresh/repeat-before-scan bootstrap, explicit inspect data root, and paused Git directory parsing; focused tests/vet passed. Controller actual Session-content CLI run: 70 + 22 turns, first/last pages and answers (f1-native/green-3); source gaps remain honest partial. Independent package review + bfeb644 scoped re-review PASS. Native goal edit → save/close → scan → reopen PASS; complete evolution/source acceptance remains open. |
| F2 Problem operations | First root, split/placement confirmation, child/sibling/merge/move/reorder, resolve/reopen, reload/rescan preservation | BEHAVIOR VERIFIED; UI IMPLEMENTED, USER ACCEPTANCE PENDING | d5a22af trusted create/edit/move/reorder/state; 6e9fbbc scan-derived private candidate reconciliation + source answer viewer/lifecycle. Native first candidate→root→readback and rescan preservation PASS; Native child/sibling confirmation, reorder, cross-parent move, resolve/reopen and merge passed on 2026-09-09. Real merge exposed nil source refs; narrow fix retested with same candidate. Ordinary rescan preserves 4 nodes, decision revision2 and all prices; Project/Vault ledger SHA dd50c1b21bce0f899c2b95c161f9f1d840c8c01b19fcace885759c39cf417b75 (`f1-native/f2-native-final.json`). Optional Agent placement through c0f0cb8/f00d194 passed native controlled-adapter configuration → explicit confirmation → candidate-only update; another running job survived explicit pane close/reopen and cancelled at revision4 with no remaining adapter process. Evidence: `f2-agent-native/completed-native-proof.json`, `cancel-native-proof.json`. Real adapter exposed valid-null JSON rejection; focused correction and same native flow passed; earlier failures retained. Final scoped integration review CLEAN; b00c709 durable publication/candidate recovery, abort/retry and merge+later-human-edit/no-replay regressions passed. Native final same-build root confirmation+completed intent+accepted receipt and rescan byte preservation passed (`f3-native-green-7/final-confirmation/proof.json`). |
| F3 Decisions and agreements | Create/edit, explicit extract/status/cancel, confirm/ignore/not-decision/restore, readback and rescan preservation | BEHAVIOR VERIFIED; UI IMPLEMENTED, USER ACCEPTANCE PENDING | 37b7fa9 manual create/edit/candidate transition publication; 6e625db verified Agent configuration. Controller native manual create→edit→explicit close/reopen→ordinary rescan PASS, decision revision2 retained. Candidate UI/CLI confirmation wired with targeted tests; Extraction/configuration/status/latest/cancel frontend committed f00d194 (48 focused tests, build/lint passed; independent scoped re-review clean). Backend ba8c315 bounded extraction baseline: actual controlled long-chain batch run completed with2 candidates (`f3-native-green-6`), zero-new-dependency request starts no worker. 254477e binds exact source revisions, preserves unchanged-view freshness, supports failed/cancelled retries and merges cross-batch evidence; e91ca85 displays the exact supporting text only after click and rejects stale confirmation (independent frontend review/re-review clean). Immutable FileStore25362fd +9d13627/dce0fab candidate-authoritative extraction reconciliation reviewed clean. Native controlled extraction→exact evidence→edit/confirm, ignore/restore, not-decision, close/reopen and rescan preservation PASS (`f3-native-green-7/native-proof.json`); unchanged repeat request starts no additional worker. 2d05c0b fixes stale evidence null-array wire failure, same native rescan flow passed. Formal publication→candidate-transition crash recovery fixed6b51c67 with immutable accepted results7cd5fbf and shared intent2edbe1d. Focused injected-crash/retry/human-edit tests and independent review CLEAN. Native same-build explicit extraction→exact evidence→confirm/save→close/reopen→rescan PASS, with candidate confirmed, intent completed and authentic archive receipt (`f3-native-green-7/final-confirmation/proof.json`). |
| F4 Usage and pricing | ModelPriceWatch fetch/cache/exact route, immutable pricing/supplement and honest offline/unknown UI | BEHAVIOR VERIFIED; UI IMPLEMENTED, USER ACCEPTANCE PENDING | Backend through ed4f15b/b9d6d21: 248 live entries, exact reviewed listing + interval/predecessor/CAS. Shared UI 09a8ee8 + follow-up review: explicit query/route confirmation/manual supplement. Native query248→acceptance-only manual price→reload→rescan PASS, Project/Vault identical ledger; exposed and fixed TS zero-quantity/null-rate completeness mismatch. Pricing-only independent review clean. Full plugin588/Go61-package integration, vet/tidy and final static review passed; this closes F4 package verification. |
| F5 Completeness and release | Private search, supported event producers, provider parity/source navigation, full regression/review/native acceptance and release targets | PENDING | Claude scan + authenticated full/retained question-answer adapter b8d7af8/ce2349a: independent review exposed repeated usage per message chunk; corrected with conflict diagnostics. Claude-only/no-Codex and invalid-root diagnostics regressions pass. Native controlled Claude JSONL30-turn first/last paging and exact turn30 answer passed; hidden reasoning absent (`f5-claude-native/native-proof.json`, screenshot). This is controlled source evidence, not historical Claude acceptance. Native controlled typed-source scan → qualified verification milestone with real projected trigger/answer/3 actions/4 results → source-full exact answer passed; missing impact stays honest (`f5-event-native-green-2/native-proof.json`). Native private branch search `main` returns1 matching Session; retained fallback separately observed before fixture source-root binding. Native original-Session launcher b07fdda + host e91ca85: explicit native Obsidian prepare/confirm → real Terminal passed with controlled provider, exact Session argv/project cwd and TTY (`f5-launch-native/summary.json`). Historical answer reader keeps historical view while launch authenticates the current index view; host review clean. Backend launcher bounded-read correction c74cd67 and scoped review PASS. Real provider-client resume remains separate. OpenCode v1.18.30 discovery still roots-only/default100/no completeness signal; read-only SQLite amendment approved 2026-09-10; adapter code, fixture regression and native30-turn/child acceptance complete; real-store schema/read-only discovery passed with0 matching Sessions; real content acceptance needs a specified OpenCode project. ModelPriceWatch catalogs live (248 matching IDs); independent preflight report in existing SDD workspace. No implicit source-contract amendment. All S01–S12 remain applicable. |

Native F1 inspection on bfeb644: real-content remapped root Sessions render in Obsidian and selected-turn retained Agent answers load; first/last70-turn pages reached. Candidate binary must have semantic buildinfo.Version or plugin discovery rejects `dev` and falls back to installed CLI. Test setup fixed without production change. Native window repaint required zoom after Force Reload; DOM inspection proved loaded data while accessibility/screenshot had stale/blank output. Native human goal edit → explicit save/close → ordinary scan → reopen passed on bfeb644; Project/Vault Markdown both preserve the marker and have identical SHA256 05819f4310d8010d45dc556850f080ec5a004d4c2c63373a17e915fb18ab7b71 at revision2. Complete coherent evolution remains open. Existing `sync status` does not attest separately loaded public snapshot, so public-only header remains read-only by design while query commands authenticate their own data.

Historical F5 source classification gap (corrected by5100079/7a99e58/1f4ca1c; current native readback passed as recorded below): source `<subagent_notification>` wrappers are materialized as user questions (e.g. root Session01a050c7... ordinals18/19/61...). Source classification must distinguish orchestration from human questions without losing real mixed/quoted content or rewriting source coordinates/historical references. This is part of source completeness; F2 rules must not recommend wrapper text as a real problem. Native historical fixture has no qualified milestones and1671 unsupported records; it does not establish production event completeness.

Ruling: use visible-turn-v2 for corrected orchestration-wrapper classification, with current scan/query wiring updated and historical documents interpreted by their retained rule version. This is a necessary segmentation correction; source coordinates/hashes remain intact and historical references must not silently read new answers. Cost if wrong: bounded rule/version correction and rescan, never historical proof rewriting.

F5 source producer correction ff81381 (independent scoped review by first_use: no blocking finding): accept bounded flat JavaScript scalar tool arguments (unquoted keys, single-quoted strings), never execute expressions. Behavioral RED showed lost verification, GREEN and full source/codex package (10.515s) + scoped vet passed. Private actual-source probe: 2221 exec wrappers, 968 now structurally supported; all emit body-only r.output, so command starts can be recovered but no trusted exit status or verified milestone may be inferred. Independent review complete.

F1 implementation ruling: a genuinely fresh init establishes the project mapping and private scaffold; the first ordinary scan owns atomic publication of the four current-format files. No fabricated empty generation and no legacy-file deletion. Existing valid/partial legacy initialization recovery is preserved. CLI help must explain the first-scan step. Explicit custom `--data-dir` must also reach read-only inspect commands so the initialized project can actually be read.

Ruling: price status `superseded` is lifecycle metadata required by §18.6; supersession may update only that status, while accepted rates, quantities, costs, source evidence and snapshot ID remain unchanged. New generated IDs exclude lifecycle status. This reconciles §8.3 immutable pricing evidence with the explicit graph contract without redesigning shared ledger storage; cost if wrong is a bounded status/ID compatibility correction.

F5 private search 5f8294a: authenticated published typed observations, literal normalized branch/file/error queries, identity-only result pages, bound cursors and no-write tests PASS; CLI boundary test + vet PASS; rendered explicit-search/clear-late-response and existing filters 54 tests PASS, scoped eslint/tsc PASS. Shared runner/host wiring and independent final review throughb00c709 are clean; native private branch search returned the exact matching Session as recorded in F5.

Ruling: decisions edit uses a bounded stdin human-fields payload with decision ID and expected Review SHA; optional absolute --data-dir follows the existing init/inspect/problem/pricing CLI convention. This adds the missing required edit entry without changing user-visible decision semantics; cost if wrong is a narrow argv compatibility adjustment.

Execution rules (simplified at the user’s request on 2026-09-10):

- Use one implementation owner for bounded changes. Implement an already-authorized small correction directly; do not create another plan, handoff or chain of reviewers for each style fix. Keep independent review at meaningful functional-package or integration boundaries, and re-review only actual findings.
- For source adapters, check actual file sizes and available schema metadata before setting fixture sizes or resource limits.
- Select checks by the changed surface. UI corrections use affected behavior tests, types/build, scoped lint and actual rendered inspection, including narrow panes when layout changes. Run full backend regressions for affected shared runtime/storage/provider changes or an integration/release gate; do not repeat them for CSS or copy.
- Reuse successful results while the relevant code is unchanged. Diagnose environmental failures separately; rerun failed/affected work instead of restarting already-passing suites. Once the required checks pass, stop verification unless a new change, failure or concrete unresolved risk justifies another check.
- Keep this single current queue and record concise outcomes in place. Small visual corrections should produce a visible code/rendering result in the same work cycle, not another audit-only status update.

One implementation owner retains each package/shared state. Early native actions use the authorized test Vault and real scanned data. Independent package backends may overlap only with disjoint declared file ownership; shared CLI/publication/UI integration remains serialized. Data loss/wrong source/security/implicit-model calls block immediately; current-package flow failures block that package; other spec requirements stay in their package and nonblocking improvements do not preempt the active package. Main human files, prototypes and paused diffs are preserved. Publishing and installation remain separately verified final outcomes after complete acceptance.

Native integration checkpoint 2026-09-09 19:37: temporary Vault ID `fc7813815118c8e6`, real historical source content with only project paths remapped, project `project-c927ddd3a91ef8d4`. Actual ModelPriceWatch query returned248; manual acceptance-only prices saved; first problem confirmed; manual decision created and edited; project tabs explicitly closed/reopened; ordinary scan repeated with `review_run_tokens=0`. Decision revision2, root question and manual pricing retained. Project/Vault ledger SHA256 both `3f8740143d9fd8704cc3a6bf97d170d2b2cab667ce8d3008a4fc27f6aacc7ea3`. Pricing validation repair reads the same already-published ledger, no evidence rewrite. Native screenshot and bounded report: existing SDD `f1-native/native-decisions.png`, `native-f2-f3-f4-summary.json`. Source coverage remains partial; no overall acceptance/release claim.

Integration evidence 2026-09-09 21:54 at e91ca85 +25362fd: plugin `npm run check` completed:40 files/588 tests PASS, TypeScript/build PASS, lint0errors (3 existing Agent-capitalization warnings). Candidate evidence, launcher argv/current-vs-historical view and host forwarding have independent scoped reviews. This is the plugin integration gate; Go store integration is still active and no whole-product/release claim follows.

Ruling: native resume targets the same provider/Session using the unique current index view for launch authentication; historical answer display remains bound to its exact historical view. Cost if wrong: launcher eligibility/identity correction only; no history rewrite. Controlled native launch configuration and plugin changes were confined to the authorized temporary Vault/private acceptance data; the transient one-use worker files used the macOS user cache and were consumed. Daily Vault content/plugin was not installed over.

Final convergence checkpoint (2026-09-09): 7a99e58 restricts interruption filtering to the exact known pure notice under visible-turn-v3; mixed/quoted/arbitrary XML remains visible. b220458 compares published conversation dependency tuples when deciding no-op publication; 1f4ca1c binds SessionView identity to segmentation version and tests v2→v3 distinct current identity, byte-exact old-chain retention, and unchanged repeat stability. Native historical rescan published `scan-dd08d1358928b0e26c6896c5f4942b8e`, with20+22 questions, last real question/Agent readback and old-view query PASS. Project review body, human patches and price snapshots survive. Earlier pre-fix diagnostic had already rematerialized the unbound historical view; this native run does not independently prove pristine v2 bytes. That boundary is proven by the clean committed scan regression. Evidence: `f5-launch-native/interruption-rescan-final/proof.json`, `native-final-readback.txt`.

Final review correction a30bedf: recovery-link version belongs to the base SessionView materializer rule, not the composed conversation version. Exact failure→success summary regression now accepts the link while retaining identity/dependency/tamper checks. Correction2d05c0b preserves empty evidence arrays when a candidate becomes stale after new-view publication; native candidate history remains readable with confirmation disabled and the accepted formal decision preserved.

Review adjudications: annotation25362fd already rejected oversized records via strictjson.Encode before publishing; b69a402 adds an explicit local64MiB guard but is not evidence of a previously exploitable unreadable-head bug. Native launcher final measured-path→exec race is recorded as the accepted practical Darwin boundary: pinned physical identity/hash, fixed argv, shell:false, and immediate revalidation; the controlled Terminal result does not attest actual Codex/Claude/OpenCode client restoration.

Full Go integration attempt at1f4ca1c finished with failures in old fresh-init-dependent apply/prepare/syncproject/reviewjob fixtures and the Gate A import inventory. Other packages completed, but this is RED, not a full-suite pass. F1 owner corrected explicit legacy/bootstrap fixtures without removing migration/corruption/pin/idempotency assertions. Gate A network-import growth corrected at3458675, exact gate PASS29.694s. apply/prepare/syncproject fixtures corrected54bd536; legacy reviewjob accepted baseline corrected39f7657; all originally failing focused lifecycle tests passed with behavioral assertions retained. Final review identified formal F2/F3 publication→candidate-CAS crash recovery; shared acceptance archive7cd5fbf, intent2edbe1d, F3 integration6b51c67 and F2 integrationb00c709 now close that code defect. Exact publication fault tests895b16d, per-package recovery tests and independent scoped re-review CLEAN. Shared candidatepublication/publicationstate race tests PASS. Final static report throughb00c709 is clean, with product/source/client limitations retained. The isolated-cache full Go run onb00c709 exited0:61 test packages passed,4 packages have no tests. Publication172.785s, scan234.915s and zerotoken148.284s passed. Vet and tidy-diff also passed. Plugin40 files/588tests/types/build passed with0 lint errors and3 existing warnings; frontend unchanged since that run. No release readiness claim.

Final integration environment (2026-09-10): the first full Go retry onb00c709 hit the default10-minute package deadline in publication, scan and zerotoken; no assertion failure was reported in that attempt. A30-minute serial retry then failed during compilation because referenced files disappeared from the shared Go build cache, including standard-library imports. Neither attempt is a pass. Historical logs remain `final-go-test-green.log` (misleading historical filename; exit1) and `final-go-test-serialized.log` (exit1). The successful final retry used a task-private GOCACHE and `go test -timeout 30m -p 1 ./...`, recorded in `final-go-test-isolated.log`; exit0 with61 test packages passing. Machine-readable result: `final-integration-result.json`. No product code was changed to address these environment failures.

## Binding acceptance matrix

| ID | Spec | Required outcome | Current evidence / gate |
|---|---|---|---|
| S01 | §3, §12.3.1 | Five top tabs in approved order; project header compact, risks/todos and coverage visible | Shell restored at 8c8614d; native five-tab navigation observed in authorized temporary Vault; d57b4e3 fixes reproduced narrow-pane clipping with clean review and native retest |
| S02 | §4, §13.3/13 | Qualified milestone list, full history, five-part closure; no raw-event homepage | Pure projector, human-preserving rebase and actual scan/private-proof publication reviewed through02e5d19; chronology/coverage through7ca8908, answer host through2429989, generated-text readability througha364a9f reviewed. Controlled typed-source native five-part closure, exact source-full answer and final milestone→exact confirmed-problem navigation passed. Historical body-only tool results still lack trusted exit status; complete real-source event coverage and real provider-client resume remain unproved |
| S03 | §4.1, §5.1, §13.14 | Visible answers plus execution/verification sources; bounded authenticated drilldown | Authenticated scan/CAS/query and historical snapshots reviewed through227af6e; shared exact-turn viewer and milestone host reviewed through2429989. Fresh CLI scan/human edit/rescan/historical/source-removal18.184s, strict TS/Chrome1200/390 and frozen plugin531/lint/types/build pass. Native exact source reading and controlled Terminal transport passed; real provider-client resume remains open under S11 |
| S04 | §5, §13.15/16 | Actual question tree, focus path/children/related nodes, orthogonal states | Formal node creation/editing, focus path/children, authenticated Q/A and orthogonal workflow/answer states implemented; native structural/state operations and ordinary rescan preservation passed. Final package and whole-branch static review throughb00c709 are clean; final Go integration61 packages passed. |
| S05 | §5.3, §18.3/5 | Deterministic pending recommendations, explicit child/sibling/merge/move/reorder CAS | Runtime and UI through c0f0cb8/f00d194: deterministic private recommendations, explicit root/child/sibling/merge/move/reorder, optional Agent request/status/cancel. Native structural controls and controlled Agent complete/reopen/cancel passed; final static review throughb00c709 is clean. Durable publication/reconciliation and native final root confirmation/rescan passed; final Go integration61 packages passed. |
| S06 | §6, §18.2/5 | Decisions/agreements, manual create/edit, candidate extract/confirm/ignore/restore | Manual v4 create/edit/native rescan passed; ba8c315 explicit bounded extraction baseline and f00d194 UI available. Concrete evidence binding and retry/freshness fixed through254477e; evidence UIe91ca85 reviewed. Immutable store/wrapper/extraction-job reconciliation reviewed throughdce0fab. Native explicit controlled extraction and lifecycle/rescan PASS; formal publication→candidate-transition crash recovery fixed6b51c67, independent review and current native confirm/reopen/rescan passed. |
| S07 | §7, §13.1/2/12 | Full index, filters, retained unavailable sources, summary/search/paged events | Local filters, summary, retained events, ordinal paging/stale recovery and retained Q/A independently reviewed; plugin473 and real CLI/browser bounded controls pass. Private search wired and independently reviewed; native exact branch match, historical/current paging and retained-source reading passed. OpenCode discovery and real provider resume remain open under S11. |
| S08 | §8, §18.6 | ModelPriceWatch cache, exact routed price, immutable snapshots, honest full-width cards | ModelPriceWatch exact catalog/cache/manual supplement/immutable pricing flow implemented and independently reviewed; actual native query248/manual acceptance/reload/rescan passed. Claude chunk-usage duplicate fix ce2349a included. Plugin integration588 tests/build/types passed; final static review clean. Final Go integration61 packages passed. |
| S09 | §2, §13.4/5/14 | Zero Agent starts on scan/read/rule organization; explicit optional candidates only | Current native historical/control rescans report0 review tokens; the original scan/read/lifecycle/repeat checkpoint kept exactly1 explicit controlled extraction call. A second explicitly started controlled extraction was used for final confirmation onb00c709; subsequent native keyboard/project/missing-CLI/missing-source checks start no additional worker. Full Gate A exposed pricing types importing network/cache; 3458675 isolates inert catalog types. Exact Gate A PASS29.694s after independent review of1 pure package/10 edges,4 structural reflection entries and3 scalar Output-field false positives; executable process-site restrictions unchanged. |
| S10 | §12.3.7/8/11 | Native Obsidian keyboard, project switching, reload, state persistence, no CLI/network/source recovery | Current candidate onb00c709: native direction-key Tab navigation, A→B→A with per-project Tab/Session/answer state, explicit pane close/reopen, actual missing-CLI recovery, and temporary missing-source retained-answer recovery PASS. Wrapper and source restored byte-identically; no additional controlled worker call. Final evolution→related exact problem navigation passed. Evidence: `f3-native-green-7/final-confirmation/native-s10.json`. Offline pricing transport is covered by scoped runtime/UI tests, not a native OS-network-disable claim. |
| S11 | §13.11 | Enabled providers use same index/drilldown/status experience | Generic memory contracts01218cb+cf81296 and retained Go/TS query contracts6dc732c reviewed, including identical native IDs and cross-provider cursor rejection. Claude adapter/source reader b8d7af8/ce2349a and controlled native30-turn parity passed; independent source review findings repaired. OpenCode opt-in complete discovery is implemented under the approved 2026-09-10 read-only SQLite amendment; fixture and real-store compatibility verification are recorded above; real content acceptance lacks a matching SessionReviewer sample. Native original-Session transport passed controlled-provider Terminal acceptance; real provider-client resume remains open. |
| S12 | §9/10, §17.4 | Human preservation, generation/preimage/CAS integrity, private source content | Retained-view version regression preserves pristine historical chain bytes; native manual edits and final formal problem/decision Review bytes survive same-source rescan. Durable accepted-publication receipt and candidate intent crash/retry tests plus scoped race tests passed; whole-branch static review clean throughb00c709. Final full Go integration61 packages passed. |

## Delivery rules

- UI-only shell restoration does not close S02–S12.
- Existing subsystem plans remain requirements; any outdated file names/interfaces are adapted to current code, never used to omit behavior.
- Save focused RED/GREEN evidence and independent review per functional package. Final whole-branch review plus actual Obsidian screenshots and action outcomes are separate gates.
- Model calls required for optional-candidate acceptance use controlled test adapters unless the user explicitly requests real model execution; do not spend user tokens as part of routine scanning.
- Do not auto-install into the real vault before candidate verification and backup; do not publish based on this repair authorization alone.

## Execution

1. Restore the v4 five-tab host and compact recovery header, retaining safe read boundaries and data-backed panel contracts.
2. Complete conversation evidence, qualified milestone projection and five-part closure.
3. Complete problem rules/store/structural CAS and approved tree/canvas/detail/drawer.
4. Finish full Session filters/summary/search/persistence and provider-neutral drilldown.
5. Complete decision/annotation commands and human confirmation UI.
6. Complete ModelPriceWatch pricing runtime and source-linked usage UI.
7. Run all code, regression, spec-matrix, independent-review and native acceptance gates; report exact remaining blockers before any release request.

## Retained summary checkpoint

- Backend summary runtime implemented at `ae742f4`; independent spec and code-quality review approved without findings. Full inspect/CLI tests and scoped vet passed in its task report; controller also verified a real retained-data read.
- Read-only query against the actual retained Session `codex / 01a06a33-fe42-77c3-b850-c4eeaa4c13fa`, generation `scan-4535e7a655a4f9d09395b30f55293de5`, succeeded on 2026-09-07. It returned 5 key operations and no captured phase/verification/error/unresolved entries, with accepted coverage 88 seen / 88 indexed. This is a real runtime read, not proof of complete conversation/execution capture.
- Root-cause follow-up for S02/S03/S09: the Codex machine-fact decoder currently excludes `function_call`/`function_call_output` and only dispatches `custom_tool_call`/`custom_tool_call_output`; its command exit-code grammar only recognizes `exit code: N`. These boundaries require fixture-backed investigation before milestone closure can claim current-host execution coverage. Do not treat zero summary verification entries as proof that no verification happened.
- S07 remains open: plugin summary wiring, filters, private search and native acceptance are not closed by this backend query.
- Controller full Go regression at code commit `ae742f4` (docs-only head `4faf02f`): `go test ./...` exited 0, including `test/zerotoken` (92.347s); `go vet ./...` and `go mod tidy -diff` exited 0 with no output. This is backend regression evidence, not a replacement for the still-open product matrix.
- Deeper real-data inspection found all5 current key-operation rows have empty text and are typed `artifact/session_started` bookkeeping. The authentication backend task passed its bounded review, but useful summary projection has a newly reproduced gap. `2026-09-07-summary-typed-fact-text.md` tracks the sequential correction; do not call these5 rows substantive operations or summary product acceptance.

## Summary UI implementation checkpoint

- `eabf81e` wires authenticated retained summaries into 全部 Sessions before Q/A and execution facts, with five compact disclosures, exact returned/total/omitted counts, and explicit missing-digest / missing-CLI / empty-excerpt states. Independent task review and test-only fix `c79b100` re-review are clean; this is not product acceptance.
- Controller independently ran `npm --prefix obsidian-plugin run check` against `eabf81e`: 25 files / 364 tests passed, lint, TypeScript and production build passed. Viewed desktop and 390-pixel component screenshots and read the keyboard/retry/stale-response harness; this evidence uses synthetic fixtures, not a real native Vault.
- Read-only metadata counting on the actual first Session segment found 1,893 `exec` wrappers: 1,428 mention `tools.exec_command` (1,300 single / 128 multiple), 449 mention `tools.apply_patch`, and 174 mention `tools.write_stdin`. Categories overlap. These counts are shape probes, not decoded execution evidence; the decoder currently ignores that wrapper name. The root-cause work must handle provenance and unknown results explicitly rather than treating wrapper completion or stdout prose as a child process exit code.
- Review follow-up: unavailable-source Q/A copy promises retained execution facts, but the UI currently blocks their loader. `2026-09-07-retained-event-access.md` tracks the capability correction with private no-write regression; source-backed Q/A remains disabled. This gap is not hidden by summary UI acceptance.

## Typed-fact summary correction checkpoint

- `ca17d16` excludes startup/cwd bookkeeping from key operations and uses closed neutral typed labels with explicit unknown/conflicting outcomes, bounded redacted excerpts and unchanged sources/coverage. Independent spec and quality review approved without findings.
- The real retained query was repeated by implementer and controller: rule `summary-typed-fact-text-v2`, key operations0/blank0, accepted coverage88/88 unchanged. No scan or stored-data change was performed; this does not recover unindexed commands or prove there were no operations/verifications.
- Implementation evidence: focused inspect tests, inspect+CLI tests, scoped vet/diff-check and `go test ./... -count=1` all exit0, including zero-token integration201.861s. Controller separately reran focused typed/bookkeeping tests (0.797s), inspected unchanged block caps/final recheck, and verified actual query output.
- At this checkpoint, main remained5c5cb7a with original dirty human documents/prototypes; installed CLI and NeoMei-Docs plugin hashes remained unchanged. Native activation was pending then; the following checkpoint supersedes that permission state.

## Authorized temporary native Vault checkpoint

- User explicitly allowed exiting Restricted Mode and enabling SessionReviewer only in `.superpowers/sdd/2026-09-07-v4-five-tab-restoration/native-ba19351/Vault`. This is not authorization to install into NeoMei-Docs, change its CLI binding, rescan, merge, push or release.
- Built f74ec14 and copied main.js/manifest.json/styles.css to that temporary Vault. Bundle SHA256 `84ab476aedcbdd121fac8dec31ce31255876a849f1d447b5d0b4234ebd42b2d9`; manifest remains development candidate0.4.2. Native Obsidian1.13.7 registered 打开项目脉络; temporary community-plugins.json confirms SessionReviewer enabled.
- Observed all five tabs and their contents, history last page2/2 with16 seeded milestones, earliest human history conclusion, seeded formal question/decision, Sessions154 with page1/7, search for session-001 and explicit selection of its detail, and usage2310tokens with cost待定. These are synthetic fixture navigation checks, not automatic milestone/closure production or a real scan.
- Native problem view exposed horizontal clipping: window984px, pane approximately584px, three minimum grid tracks remain active because CSS tests viewport width. Right question evidence is cut off. `2026-09-07-v4-pane-responsive.md` tracks a real-renderer narrow-pane regression and scoped repair; browser viewport-only checks had missed this defect.
- Filtering the Session list retains the previously selected detail until another row is clicked. This native evidence belongs to the already planned Session-filter/selection-state task; it is not fixed by the responsive repair.
- Temporary project `project-gate-b-test` is not authenticated in the installed CLI data root; the native UI accurately shows 同步状态验证失败 / 待私有验证·只读. Authenticated summary/Q&A/event drilldown, A-B-A project switching, real scan preservation and complete keyboard flow remain unverified. Do not change daily configuration to make a fixture pass.

## Native pane layout correction

- `d57b4e3` adds a named v4 inline-size container and scopes compact layouts to actual pane width. Legacy viewport rules and runtime data/selection/authentication are unchanged. The browser fixture resides in `obsidian-plugin/tests/fixtures` so existing typed lint applies without rule suppression.
- RED: real renderer, viewport1280/host580, failed with `detail escapes the shell on the right`. GREEN: host580/760 stacks tree→context→detail, host1200 preserves three columns, viewport390/host374 fits, wide→narrow→wide retains selected tab/node; nested Session/conversation/event and usage/header checks pass. Controller independently reran the committed script, exit0, screenshots `/var/folders/0n/49qgdd8x7kgcvh719fw743mh0000gn/T/session-reviewer-v4-pane-joz6qE`.
- Implementer fresh full `npm --prefix obsidian-plugin run check`: exit0, lint clean,25 files/364 tests passed, TypeScript and production build passed. Browser fixture checks page identity, meaningful content, overlay absence and console health; native console was not inspected.
- Temporary Vault's previous styles were backed up in this plan's ignored workspace. Installed candidate CSS SHA256 `f0c9a0d09b4ab27e9067ca6923d92f38f434c431968b395e419e2367ce14ad51`; main.js remains unchanged. Reloaded native Obsidian1.13.7 with only the read-only project pane open; Problems tab remained selected. The same984px window now stacks the three regions. Clicking the selected question then pressing Tab reached the native Markdown button and scrolled the complete detail into view: completion criterion, conclusion, answer state and source copy are readable without right clipping.
- Native coordinate scrolling intermittently returned `noWindowsAvailable`; AX click + keyboard Tab provided the actual successful navigation evidence. No native wheel-scrolling claim is made.
- Daily NeoMei-Docs main.js/styles.css and installed CLI hashes match the pre-repair values. This closes the reproduced clipping defect, not S02–S12 or full product acceptance. No main merge, GitHub push or release was performed.
- Independent task review of ee71034..d57b4e3: spec compliant, quality approved, no Critical/Important/Minor findings. Full-branch review remains a later gate after the other restoration tasks.

## Session local navigation restoration in progress (2026-09-08)

- Baseline e65c056: focused state/records/shell suite3 files/32 tests passed, but native temporary Vault confirms missing assertions: query `session-001` leaves one matching row while detail remains `codex/session-154`; query `not-present-fixture` removes all rows but retains the same old detail. This is a selection consistency bug, not missing source data.
- The accepted `2026-09-07-session-index-filters-restoration.md` task restores local provider/date/processing/source filters, bounded25-row first/last navigation, and per-project selection persistence. No private branch/file/error search is implied. Preserve full accepted coverage independently of filtered counts.
- Boundary audit found full-state saves from stale leaves could overwrite another leaf's newer fields. The bounded correction forwards changed fields and merges into the latest per-project state; main.ts serialization, machine schema and source-reading contracts are unchanged. Both directions require host tests.
- Completion gates: isolated behavioral RED for stale detail and empty-result disposal;154-row range/last-page checks; same-ID different-provider selection; date boundary/error behavior; typed saved-state validation; focus/caret stability and no duplicate authenticated loaders; tab return, index refresh, A→B→A and two-leaf patch tests; full plugin check and desktop/narrow rendered QA. Implementation and independent review are still pending at this checkpoint.

## Session navigation controller verification (2026-09-08)

- Implementation `eae9683` restores the bounded local filters/navigation/state path. Controller independently ran `npm --prefix obsidian-plugin run check`: lint clean,26 files/380 tests passed, TypeScript and production build exit0. Independent task review is in progress; this is not product acceptance.
- Controller reran the committed pane-layout regression: exit0, screenshot directory `/var/folders/0n/49qgdd8x7kgcvh719fw743mh0000gn/T/session-reviewer-v4-pane-2N4DAu`. External Browser plugin is unavailable; regular Playwright uses existing bundled dependencies without installation. The temporary harness at `http://127.0.0.1:8765/` mounts the actual host with synthetic fixtures, not a real scan.
- Added strict assertions to external `/tmp/session-reviewer-filter-qa.OYTCsf/qa.mjs`; both580px pane and390px viewport pass page identity, meaningful content, zero overlays/console warnings/errors,25-row first page,4-row last page, empty results with no stale detail, query-selected detail, tab-return persistence, focus and zero body/pane horizontal overflow. Controller inspected both screenshots. The narrow top-tab strip scrolls horizontally; zero body overflow does not mean all tabs are simultaneously visible.
- Additional actual-control assertions reproduced a defect missed by the initial suite: after selecting Provider `claude` and clearing filters, results return to154 but the Provider select still displays `claude`. `/tmp/session-reviewer-filter-qa.OYTCsf/filter-controls.mjs` fails its visible-control assertion. Before that assertion, provider/state/availability AND, inclusive Asia/Shanghai dates, invalid backwards-date retention and unknown-only filters passed. Narrow TDD correction dispatched; do not close the task until the same script passes.
- Native AX inspection still shows the old bundle and original stale detail. Temporary-Vault bundle SHA256 remains `84ab476aedcbdd121fac8dec31ce31255876a849f1d447b5d0b4234ebd42b2d9`, CSS `f0c9a0d09b4ab27e9067ca6923d92f38f434c431968b395e419e2367ce14ad51`. No candidate install occurred in this task. Current-build native acceptance remains open; the earlier permission to enable the temporary plugin does not establish acceptance of this build.
- Main retains its existing dirty human review documents and brainstorm directory; this work remains isolated on `codex/spec-ui-restoration`. No merge, push, release or daily-Vault install performed.

## Session local-navigation task closure

- `6010623` fixes the controller-reproduced clear-filter mismatch by deriving the Provider select exclusively from applied state. Isolated RED reproduced `expected '' received 'claude'`; GREEN regression asserts visible selects, restored rows, eligible selected detail and exact persisted filters together. Independent original-range review identified only this Important issue; bounded re-review of `eae9683..6010623` is APPROVED with no remaining scoped findings.
- Controller fresh full plugin check after the fix: lint clean,26 files/381 tests passed, TypeScript and esbuild exit0. Rebuilt the external harness from the corrected source before repeating both scripts: `qa.mjs` strict assertions pass at both widths, and `filter-controls.mjs` passes the exact Provider-clear failure plus combined filters, local-date boundaries, invalid-date retention and unknown-date controls. No test expectation was weakened to hide the defect.
- Bounded local-filter task is complete at code6010623. Current-build native installation/acceptance, S07 authenticated private search and actual provider parity remain open. S02–S06 semantic workflows and S08 pricing remain separate blockers; this checkpoint does not certify the whole branch or authorize release.
