# SessionReviewer 0.4.2

## 本次修复

- 恢复 Obsidian 项目切换与 v4 扫描记录展示，新扫描内容不再因旧视图而不可见。
- 增加按需认证的用户／Agent 可见问答，支持问题列表与消息两级分页，长 Session 不限于最近十几条。
- 展开回答正文，区分 Agent 过程说明与最终回答；回答存在不等于执行已验证或问题已解决。
- Codex 的补充 token_usage_record 作为已知元数据处理，消除误报且不重复计费。
- 加固查询身份、源前缀、分页游标、错误脱敏、异步切换及索引摘要／正文截断状态。

## 升级

CLI 与插件统一为 0.4.2；数据格式最低读写能力仍为 0.4.1，不要求新格式迁移。
BRAT 使用新的 0.4.2 标签及独立 main.js、manifest.json、styles.css 发布资产识别升级。
请同时升级本地 CLI 与插件；安装本身不自动扫描。旧扫描中的未解码统计需明确重扫才能更新。
备份人工回顾和本地接受状态，不覆盖或删除人工内容。

## 覆盖边界

本次恢复查询支持 Codex 可见用户／Agent 内容，使用已发布的认证源前缀；不显示隐藏推理、加密内容或工具原始输出，不调用模型，不向 Vault 持久化完整聊天正文。
单源记录读取上限 64 KiB，超限会如实报告，不能把超限记录数等同于缺失回答数。
问题拆分／归位推荐、后续语义摘要及真实旧项目迁移不属于本补丁交付范围。

## 验证

修复代码 84a1b3b 经独立整分支审查和定点复审，无未关闭阻塞。
完整 Go 测试、vet、依赖一致性、插件 322 项测试、lint 与生产构建通过。
真实 Obsidian 验证项目切换、插件重载、首／中／末页和长问答最终回答。
目标 Session 重扫后 0 问题、0 Token；78 组问答、1,606 条消息全页源引用校验通过，正文截断 0，项目与 Vault 文件一致。
发布标签 CI 和资产校验为独立门禁，不由以上本地结果替代。详见 [验收记录](../verification/2026-09-07-visible-conversation-recovery.md)。
