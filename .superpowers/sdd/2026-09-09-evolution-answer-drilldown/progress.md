# SDD ledger — plan: docs/superpowers/plans/2026-09-09-evolution-answer-drilldown.md

Task 1: complete (commits82040d7..b99b6da, review clean). Independent review_fixed_turn_viewer approves spec and quality, zero findings. Controller resolves unverifiable seams: committed report contains owned RED/GREEN and final516-test full check, controller real CLI and Chrome checks recorded above; unchanged contracts-v4.ts::parseStrictWireDocument/documentObject still executes the64MiB byte limit, Unicode and duplicate-key rejection before the newly shared validator. Existing parser/CLI50 tests pass. Native and milestone button remain Task2/umbrella gates. Additional final Chrome includes real long full/retained first pages1200/390 with no overflow/console errors, complementing seven-page exact-order DOM traversal; screenshot inspected.

Task1: independent review active atb99b6da671829776413c23796a04921facd781e3, BASE82040d7, reviewer /root/review_fixed_turn_viewer(sol/high),84444-byte exact package. Worker final npm check29files/516tests/lint/types/build and diff check pass. Controller latest fixed browser pass after final self-review; additional actual CLI long-turn lifecycle7.979s exports seven limit20 pages for120 long assistant replies in both full/retained states. Rebuilt fixed-turn-long-controller reads all120 in exact order, verifies121-message ranges plus first/last/previous opaque cursor navigation and disposal; exit0. No new native acceptance, no button yet. Task2 dispatch still waits for independent review.

Task 1: active; clean BASE82040d7dba484d4c6a12438a30f18c6873e2d1ac, sole fresh implementer /root/fixed_turn_viewer(sol/high). evolution-presentation-integrity Task1 is independently approved through7ca8908 with no findings; docs checkpoint82040d7. Task2 remains pending until Task1 independent review. Main remains outside this worktree; no publication or native daily-Vault installation is authorized in these bounded task commits.

Controller intervention during Task1: worker initially proposed fixed-mode-only object validation after old index fixtures failed; rejected because both modes must share validation under the brief. Confirmed old fixture has visible_messages2 plus oversized_records2 greater than source_records2 and three turns with stale visible/captured counts. Worker will mechanically correct existing render-conversation.test.ts positive fixtures to structurally valid parsed pages (counts, ordinals, selected metadata, cursor topology) and restore shared runtime validator in both modes; negative tests must mutate valid fixtures after parsing to exercise renderer rejection with positive controls. No validator weakening is authorized.

Controller mid-implementation GREEN: original fixed-turn-controller probe rebuilt and passes real current/historical CLI responses with exact turn request, original answer, retained warning, disposal. Real Chrome fixed-turn-browser passes1200/390 exact current/history, malformed provider rejection, keyboard retry, delayed disposal and no overflow/console errors; historical390 screenshot inspected. Existing controller-published-browser four-state Session host replay and historical-cli-browser current/history/current keyboard replay also pass with shared validation. These are real CLI response replays with controlled transport, not new native scanning or feature acceptance. Worker full plugin/default-mode fixture checks and review remain pending.

Ruling: Use fixed-mode-specific heading copy instead of inheriting the default viewer's reference to a nonexistent below-panel indexed event list — the milestone expansion has no such list — cost if wrong is a small local copy correction; default Session copy/API/layout remain unchanged. Worker must add an owned assertion.

Task1 behavior-ready controller rerun after shared-validation restoration and fixed-heading change: fixed-turn-controller + fixed-turn-browser pass again with1200/390 screenshot inspection; ordinary four-source-state Session host and current/historical/current keyboard replay also pass. Worker focused85 passed (fixed21/default14/parser-CLI50), full plugin check/report/commit pending. No answer button has been integrated yet.

Task 1 independent RED also recorded at7ca8908: fixed-turn-controller.ts parses actual CLI current/historical responses and invokes the real viewer with the exact published turn selector. First request incorrectly omits turnUnitId and requests the Session index. Assertion fails as expected (exit1); later body/retained/disposal assertions remain unexecuted, not failed product behavior. Rebuild with esbuild external jsdom before future GREEN.

| Shared surface / task | Producer / consumer | Preflight finding |
|---|---|---|
| Task 1 self-consistency | existing conversation viewer / optional fixed turn | fixture functions exist; full initial selected page must be authenticated before setting selectedTurn; no index request is needed |
| Task 1 validator extraction | Node strict-wire wrapper / browser renderer | contracts-v4 imports Node hashing; share only the structural object validator and preserve strict wire checks and UTF-8 byte limits |
| Task 1 / Task 2 | optional fixed-turn renderer / explicit answer click | compatible default API, shared paging/security guards, disposed viewer must clear retained body references |
| Task 2 self-consistency | accepted conclusion refs + index / actual v4 shell | existing loadConversation is available at shell, absent at evolution; add optional final argument and disposal without changing legacy callers |
| Task 2 / existing bindings | qualified historical refs / authenticated current generation | current index view may differ or be null; query the uniquely accepted evidence snapshot, never latest or the first turn |
| Task 2 / remaining closure actions | answer expansion / original source and related problem | this plan does not implement the other two actions or establish native acceptance |

Ruling: Reuse the existing conversation viewer with an optional fixed-turn mode instead of creating a second pagination/security implementation — existing Session browsing remains the default — cost if wrong is a bounded shared-renderer refactor with both mode regressions before integration.

Controller preflight 2026-09-09: actual temporary-source CLI lifecycle passed6.981s, exporting both current full-body and historical retained-excerpt responses with their own matching accepted presentation/ledger/index. External evolution-answer-browser.mjs validates both bundles with actual strict TypeScript guards and verifies current/historical body availability; real Chrome reaches five top tabs and zero-initial-loader controls, then fails exactly at absent 查看回答正文 button (0 versus1). Later interaction assertions are unexecuted until implementation; no claimed UI GREEN. Temporary artifacts: /tmp/session-reviewer-scan-chain-qa.6VBnCG, no product fixture imports from that directory.
